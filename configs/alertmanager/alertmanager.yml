global:
  resolve_timeout: 5m

route:
  receiver: 'default'
  group_by: ['alertname', 'http_method', 'http_route']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 30m

receivers:
  - name: 'default'
    webhook_configs: []
